/**
 * @license
 * SPDX-License-Identifier: Apache-2.0
 */

// SVG sample presets have been removed.
// The block now starts with an empty canvas when no saved config exists.
// Users upload their own SVG files via the builder interface.
